<!-- For product's search, but ommitting for project. use for future -->
<% if false %>
<h1>Product search</h1>
<%= form_tag(searchproducts_path, id: "psearch_form") do %>
  <%= text_field_tag :search, params[:search] %>
  <%= submit_tag "Search", name: nil %>
<% end %>
<% end %>

<h3 class="text-center">Products for Sale</h3>
<div id="results">
  <!-- renders the Products for Sale table -->
  <%= render 'presults' %>
</div>

