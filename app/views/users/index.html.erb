<h1>User search</h1>
<%= form_tag(searchuser_path, remote: true, id: "search_form") do %>
  <%= text_field_tag :search, params[:search] %>
  <%= submit_tag "Search", username: nil %>
<% end %>

<div id="results">
  <%= render 'results' %>
</div>
